

var foo = require("foo.js"); // 没有写 ./  Node.js默认视它为 node_modules目录下的模块

var foo2 = require('foo'); //可以省略.js

console.log(foo.msg);
console.log(foo2.msg);

var bar = require('bar'); // 找到目录中的index.js文件
console.log(bar.msg);


var temp = require('temp'); // 目录下没有index.js文件, 就去package.json中找main对应的文件.
// package.json文件要放到模块的根目录下.
console.log(temp.msg);
